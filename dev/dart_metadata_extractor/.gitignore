bin
result.json

# Extractor output: regenerate with the tool in lib/ rather than committing it.
# The committed dumps carried the absolute paths of the machine that produced
# them — noise, and a needless disclosure. (Supersedes the old result.txt rule.)
*.txt
